import { v4 } from 'uuid'

import Verification from "./models/verifySchema"

const generate = () => {
  const verifyToken = v4()

  return
}

export default {
  generate,
  // save,
  // remove,
  // validate,
  // search,
}